rare [--json] configure [--chain <mainnet|sepolia|base|base-sepolia>] [--private-key <0xPRIVATE_KEY> | --private-key-ref <op://vault/item/field>] [--rpc-url <url>] [--default-chain <mainnet|sepolia|base|base-sepolia>] [--show]
rare status --contract <address> [--token-id <id>] [--chain <chain>]

rare wallet generate [--chain <chain>] [--save]
rare wallet address [--chain <chain>]

rare search tokens [--chain <chain>] [--query <text>] [--owner <address>] [--mine] [--per-page <n>] [--page <n>]
rare search auctions [--chain <chain>] [--state <state>] [--owner <address>] [--query <text>] [--per-page <n>] [--page <n>]
rare search collections [--chain <chain>] [--query <text>] [--per-page <n>] [--page <n>]

rare deploy erc721 <name> <symbol> [--max-tokens <number>] [--chain <chain>]
rare deploy liquid-edition <name> <symbol> [--curves-file <path>] [--curve-preset <preset>] [--write-curves-file <path>] [--initial-rare-liquidity <amount>] [--preview] [--yes] [--token-uri <uri>] [--description <description>] [--image <path>] [--video <path>] [--tag <tag>] [--attribute <attr>] [--chain <chain>]
rare import erc721 --contract <address> [--chain <chain>]

rare currencies [--chain <chain>]

rare collection create lazy-batch-mint <name> <symbol> [--max-tokens <number>] [--chain <chain>] [--chain-id <id>]
rare collection create sovereign <name> <symbol> [--max-tokens <number>] [--contract-type <type>] [--chain <chain>]
rare collection create lazy-sovereign <name> <symbol> --max-tokens <number> [--contract-type <type>] [--chain <chain>]
rare collection mint --contract <address> [--token-uri <uri>] [--name <name>] [--description <description>] [--image <path>] [--video <path>] [--tag <tag>] [--attribute <attr>] [--to <address>] [--royalty-receiver <address>] [--chain <chain>]
rare collection mint-batch --contract <address> --base-uri <uri> --token-count <number> [--chain <chain>]
rare collection prepare-lazy-mint --contract <address> --base-uri <uri> --token-count <number> [--minter <address>] [--chain <chain>]
rare collection creator --contract <address> --token-id <id> [--chain <chain>]
rare collection royalty status --contract <address> --token-id <id> [--sale-price <raw>] [--chain <chain>]
rare collection royalty set-default-receiver --contract <address> --receiver <address> [--chain <chain>]
rare collection royalty set-token-receiver --contract <address> --token-id <id> --receiver <address> [--chain <chain>]
rare collection metadata status --contract <address> [--chain <chain>]
rare collection metadata update-base-uri --contract <address> --base-uri <uri> [--chain <chain>]
rare collection metadata update-token-uri --contract <address> --token-id <id> --token-uri <uri> [--chain <chain>]
rare collection metadata lock-base-uri --contract <address> [--chain <chain>]


rare auction create --contract <address> --token-id <id> --starting-price <amount> --duration <seconds> [--type <type>] [--start-time <seconds>] [--currency <currency>] [--split <addr=ratio>] [--chain <chain>]
rare auction bid --contract <address> --token-id <id> --amount <amount> [--currency <currency>] [--chain <chain>]
rare auction settle --contract <address> --token-id <id> [--chain <chain>]
rare auction cancel --contract <address> --token-id <id> [--chain <chain>]
rare auction status --contract <address> --token-id <id> [--chain <chain>]

rare list-collections [--chain <chain>] [--query <text>]

rare offer create [--contract <address>] [--token-id <id>] [--collection <address>] --amount <amount> [--currency <currency>] [--no-auto-approve] [--chain <chain>]
rare offer cancel [--contract <address>] [--token-id <id>] [--collection <address>] [--currency <currency>] [--chain <chain>]
rare offer accept [--contract <address>] [--collection <address>] [--buyer <address>] --token-id <id> --amount <amount> [--currency <currency>] [--split <addr=ratio>] [--no-auto-approve] [--chain <chain>]
rare offer status [--contract <address>] [--token-id <id>] [--collection <address>] [--buyer <address>] [--account <address>] [--currency <currency>] [--chain <chain>]
rare utils tree build --input <path> [--format <format>] [--chain-id <id>] [--output <path>]
rare utils tree proof --input <path> --contract <address> --token-id <id> [--format <format>] [--chain-id <id>] [--output <path>]
rare utils tree verify --input <path> --contract <address> --token-id <id> [--proof <path>] [--root <bytes32>] [--format <format>] [--chain-id <id>]
rare listing batch offer create [--root <bytes32>] [--input <path>] [--format <format>] [--chain-id <id>] --amount <amount> [--currency <currency>] --expiry <seconds> [--chain <chain>]
rare listing batch offer revoke [--root <bytes32>] [--input <path>] [--format <format>] [--chain-id <id>] [--chain <chain>]
rare listing batch offer accept --creator <address> --proof <path> [--root <bytes32>] --contract <address> --token-id <id> [--split-recipient <address>] [--split-ratio <percent>] [--no-auto-approve] [--chain <chain>]
rare listing batch offer status --creator <address> [--root <bytes32>] [--input <path>] [--format <format>] [--chain-id <id>] [--chain <chain>]
rare listing batch auction create [--root <bytes32>] [--input <path>] [--format <format>] [--chain-id <id>] --reserve <amount> [--currency <currency>] --duration <seconds> [--split-recipient <address>] [--split-ratio <percent>] [--no-auto-approve] [--chain <chain>]
rare listing batch auction cancel [--root <bytes32>] [--input <path>] [--format <format>] [--chain-id <id>] [--chain <chain>]
rare listing batch auction bid --creator <address> --proof <path> [--root <bytes32>] --contract <address> --token-id <id> --amount <amount> [--currency <currency>] [--no-auto-approve] [--chain <chain>]
rare listing batch auction settle --contract <address> --token-id <id> [--chain <chain>]
rare listing batch auction status --contract <address> --token-id <id> [--creator <address>] [--root <bytes32>] [--input <path>] [--proof <path>] [--format <format>] [--chain-id <id>] [--chain <chain>]
rare utils merkle proof --root <path> --contract <address> --token-id <id> [--buyer <address>] [--output <path>]
rare listing batch create --root <path> [--yes] [--chain <chain>] [--chain-id <id>]
rare listing batch cancel --root <hexOrPath> [--chain <chain>] [--chain-id <id>]
rare listing batch buy --proof <path> --creator <address> --currency <currency> --amount <amount> [--chain <chain>] [--chain-id <id>]
rare listing batch set-allowlist --root <hexOrPath> [--allowlist-root <hex>] [--end-timestamp <unix>] [--chain <chain>] [--chain-id <id>]
rare listing batch status --root <hexOrPath> --creator <address> [--contract <address>] [--token-id <id>] [--proof <path>] [--chain <chain>] [--chain-id <id>]
rare listing release configure --contract <address> --price <amount> --max-mints <number> [--currency <currency>] [--start <time>] [--split <addr=ratio>] [--chain <chain>] [--chain-id <id>]
rare listing release mint --contract <address> [--quantity <number>] [--currency <currency>] [--price <amount>] [--proof <file>] [--recipient <address>] [--chain <chain>] [--chain-id <id>] [--no-auto-approve]
rare listing release allowlist build --input <file> [--format <format>] [--output <file>]
rare listing release allowlist proof --input <file> --account <address> [--output <file>]
rare listing release allowlist set --contract <address> --end-timestamp <time> [--input <file>] [--root <bytes32>] [--chain <chain>] [--chain-id <id>]
rare listing release allowlist clear --contract <address> [--chain <chain>] [--chain-id <id>]
rare listing release limits set-mint --contract <address> --limit <number> [--chain <chain>] [--chain-id <id>]
rare listing release limits set-tx --contract <address> --limit <number> [--chain <chain>] [--chain-id <id>]
rare listing release staking set-minimum --contract <address> --minimum <rare> [--end-timestamp <time>] [--chain <chain>] [--chain-id <id>]
rare listing release status --contract <address> [--account <address>] [--chain <chain>] [--chain-id <id>]
rare listing create [--contract <address>] [--token-id <id>] [--collection <address>] [--price <amount>] [--amount <amount>] [--currency <currency>] [--target <address>] [--split <addr=ratio>] [--no-auto-approve] [--chain <chain>]
rare listing cancel [--contract <address>] [--token-id <id>] [--collection <address>] [--target <address>] [--chain <chain>]
rare listing buy [--contract <address>] [--collection <address>] [--seller <address>] --token-id <id> --amount <amount> [--currency <currency>] [--no-auto-approve] [--chain <chain>]
rare listing status [--contract <address>] [--token-id <id>] [--collection <address>] [--seller <address>] [--account <address>] [--target <address>] [--chain <chain>]

rare swap buy --token <address> --eth <amount> --min-out <amount> --commands <hex> --inputs-file <path> [--recipient <address>] [--deadline-seconds <seconds>] [--chain <chain>]
rare swap sell --token <address> --amount <amount> --min-out <amount> --commands <hex> --inputs-file <path> [--recipient <address>] [--deadline-seconds <seconds>] [--chain <chain>]
rare swap swap --token-in <address> --amount-in <amount> --token-out <address> --min-out <amount> --commands <hex> --inputs-file <path> [--recipient <address>] [--deadline-seconds <seconds>] [--chain <chain>]
rare swap buy-token --token <address> --eth <amount> [--slippage-bps <bps>] [--min-out <amount>] [--quote-only] [--yes] [--recipient <address>] [--deadline-seconds <seconds>] [--chain <chain>]
rare swap sell-token --token <address> --amount <amount> [--slippage-bps <bps>] [--min-out <amount>] [--quote-only] [--yes] [--recipient <address>] [--deadline-seconds <seconds>] [--chain <chain>]
rare swap buy-rare --eth <amount> [--slippage-bps <bps>] [--min-rare-out <amount>] [--quote-only] [--yes] [--recipient <address>] [--deadline-seconds <seconds>] [--chain <chain>]
rare batch offer create [--root <bytes32>] [--input <path>] [--format <format>] [--chain-id <id>] --amount <amount> [--currency <currency>] --expiry <seconds> [--chain <chain>]
rare batch offer revoke [--root <bytes32>] [--input <path>] [--format <format>] [--chain-id <id>] [--chain <chain>]
rare batch offer accept --creator <address> --proof <path> [--root <bytes32>] --contract <address> --token-id <id> [--split-recipient <address>] [--split-ratio <percent>] [--no-auto-approve] [--chain <chain>]
rare batch offer status --creator <address> [--root <bytes32>] [--input <path>] [--format <format>] [--chain-id <id>] [--chain <chain>]
rare batch auction create [--root <bytes32>] [--input <path>] [--format <format>] [--chain-id <id>] --reserve <amount> [--currency <currency>] --duration <seconds> [--split-recipient <address>] [--split-ratio <percent>] [--no-auto-approve] [--chain <chain>]
rare batch auction cancel [--root <bytes32>] [--input <path>] [--format <format>] [--chain-id <id>] [--chain <chain>]
rare batch auction bid --creator <address> --proof <path> [--root <bytes32>] --contract <address> --token-id <id> --amount <amount> [--currency <currency>] [--no-auto-approve] [--chain <chain>]
rare batch auction settle --contract <address> --token-id <id> [--chain <chain>]
rare batch auction status --contract <address> --token-id <id> [--creator <address>] [--root <bytes32>] [--input <path>] [--proof <path>] [--format <format>] [--chain-id <id>] [--chain <chain>]
rare batch create --root <path> [--yes] [--chain <chain>] [--chain-id <id>]
rare batch cancel --root <hexOrPath> [--chain <chain>] [--chain-id <id>]
rare batch buy --proof <path> --creator <address> --currency <currency> --amount <amount> [--chain <chain>] [--chain-id <id>]
rare batch set-allowlist --root <hexOrPath> [--allowlist-root <hex>] [--end-timestamp <unix>] [--chain <chain>] [--chain-id <id>]
rare batch status --root <hexOrPath> --creator <address> [--contract <address>] [--token-id <id>] [--proof <path>] [--chain <chain>] [--chain-id <id>]
