{
  "name": "rare",
  "version": "0.1.0",
  "description": "CLI tool for interacting with the RARE protocol smart contracts",
  "type": "module",
  "bin": {
    "rare": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
